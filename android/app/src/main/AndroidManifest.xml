<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ================================================================
         PERMISSIONS
         ================================================================ -->
    <!-- Network: required for LTE/eSIM standalone operation & Firebase -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Location: required for Proximity Vibe feature -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- Background location: needed for proximity tracking when watch screen is off -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <!-- Foreground service: required to stream GPS in the background on Android 9+ -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />

    <!-- Vibration: required for Haptic Morse Code -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Microphone: required for Whisper PTT and Love Notes -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Wake lock: keeps tile service alive for incoming haptic signals -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Receive FCM messages in background -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="com.google.android.c2dm.permission.RECEIVE" />

    <!-- Post notifications (Android 13+ / Wear OS 4) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Body sensors: detect off-body state to auto-lock tile -->
    <uses-permission android:name="android.permission.BODY_SENSORS" />

    <!-- ================================================================
         HARDWARE FEATURES
         Marks this as a Wear OS app — STANDALONE (no companion required)
         ================================================================ -->
    <uses-feature android:name="android.hardware.type.watch" />

    <application
        android:label="Affinity"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:theme="@style/LaunchTheme"
        android:allowBackup="false"
        android:supportsRtl="true">

        <!-- ============================================================
             STANDALONE WEAR OS DECLARATION
             Tells the system this app does NOT require a paired phone.
             Required for eSIM/LTE standalone operation.
             ============================================================ -->
        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="true" />

        <!-- Flutter embedding version -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- ============================================================
             MAIN ACTIVITY
             ============================================================ -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ============================================================
             FCM SERVICE
             Handles incoming haptic-signal and mood push messages.
             TODO: Implement AffinityMessagingService in Phase 2.
             ============================================================ -->
        <!--
        <service
            android:name=".AffinityMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
        -->

        <!-- ============================================================
             FOREGROUND SERVICE FOR BACKGROUND LOCATION (Phase 4)
             Geolocator uses this to stream location when screen is off.
             Required on Android 9+ and Wear OS 3+.
             The ForegroundNotificationConfig in LocationService provides
             the notification title/text shown to the user.
             ============================================================ -->
        <service
            android:name="com.baseflow.geolocator.service.GeolocationService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="location" />
        <!-- ============================================================
             WEAR OS COMPLICATION SERVICES (Phase 6 — TODO)
             Requires adding androidx.wear.watchface:watchface-complications-data-source
             to build.gradle.kts dependencies before enabling these.
             Tap actions: ACTION_HEARTBEAT (❤️), ACTION_WHISPER (🎤)
             ============================================================ -->

        <!-- WorkManager initializer (for offline sync retry) -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup" />
        </provider>

    </application>

    <!-- Required to query activities that can process text (Flutter engine) -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/plain" />
        </intent>
    </queries>

</manifest>
